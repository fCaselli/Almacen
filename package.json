{
  "name": "almacen-pro-backend-tests",
  "version": "2.0.0",
  "type": "module",
  "description": "Sistema administrativo para almacén con backend refactorizado por capas usando Node, Express y MongoDB.",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "check": "node scripts/check-syntax.mjs",
    "test": "node --test tests/**/*.test.js",
    "test:watch": "node --test --watch tests/**/*.test.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^5.1.0",
    "mongodb": "^6.15.0"
  }
}